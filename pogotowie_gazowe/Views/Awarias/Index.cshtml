@model IEnumerable<pogotowie_gazowe.Awaria>

@{
    ViewBag.Title = "Awarie";
}

<h2>Awarie</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Powód)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Miejsce)
        </th>
     
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Powód)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Miejsce)
        </td>
  
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Awarii }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Awarii }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Awarii })
        </td>
    </tr>
}

</table>
